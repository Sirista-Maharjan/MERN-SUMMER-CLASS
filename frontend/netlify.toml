[build]
  command = "npm run build"
  publish = "dist"

# React Router uses client-side routing, so any direct navigation to a
# route like /login must still be served index.html and let the app's
# router take over — otherwise Netlify returns a 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
